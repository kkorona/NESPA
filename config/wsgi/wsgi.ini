[uwsgi]
chdir = /opt/NESPA
module = config.wsgi.deploy:application
home = /opt/NESPA/venv

uid = deploy-user
gid = deploy-user

http = :8000

enable-threads = true
master = true
vacuum = true
pidfile = /tmp/mysite.pid
logto = /var/log/uwsgi/@(exec://date +%%Y-%%m-%%d).log
log-reopen = true
